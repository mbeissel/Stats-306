# Stats-306
final project
Open the .rmd file using Rstudio.  Place the two .gz file and .xml file in the same folder as the rmd. file and set the working directory to that folder. Run code chunks selectively to view the data analysis tools used or run the entire file to initiate the shiny app
